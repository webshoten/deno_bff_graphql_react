type Mutation {
  createUser(name: String!): User
}

type Post {
  content: String
  id: ID
  title: String
}

type Query {
  post(id: ID!): Post
  postCount: Int
  posts: [Post!]
  user(id: ID!): User
  userCount: Int
  users: [User!]
}

type User {
  id: ID
  name: String
}